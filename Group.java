public class Group {
    //Can define Group as a set of 4 length which implies all colors are present
    //Or can define it as a set of 3 length with which color is missing
    public int number;
    public int[] color;
    public boolean valid;
    public Group(Tile[] inputSet){

    }
}
